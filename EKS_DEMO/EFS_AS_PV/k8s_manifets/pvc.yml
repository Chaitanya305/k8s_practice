apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: efs-pvc
spec:
  storageClassName: efs-sc
  accessModes:
    - ReadWriteMany   #for aws we cant use ReadWriteMany becouse we cant have same EBS volume attached to multi nodes
  resources:
    requests:
      storage: 1Gi